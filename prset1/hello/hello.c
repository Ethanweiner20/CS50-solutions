#include <cs50.h>
#include <stdio.h>

int main(void)
{
    // Get user's name
    // string name = get_string("Your name: ");

    // // Print hello, "name"
    // printf("hello, %s\n", name);

    string s = "AB";
    string t = "CD";
    printf("%i", s[3]);
    return 1;
}